@model Library.Models.LoginModel
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<div class="login-form">
    <div class="text-danger font-weight-bold">
        @ViewData["ReservationRequirement"]
    </div>

    @using (Html.BeginForm("Login", "Account", FormMethod.Post))
    {
        <div class="form-group">
            @Html.DisplayNameFor(item => item.Email)
            @Html.TextBoxFor(item => item.Email)
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>

        <div class="form-group">
            @Html.DisplayNameFor(item => item.Password)
            @Html.PasswordFor(item => item.Password)
            <span asp-validation-for="Password" class="text-danger"></span>
        </div>
        @Html.ValidationMessage("Error", new { @class = "text-danger font-weight-bold" }) <br />
        <input type="submit" value="Zaloguj" />
    }
</div>